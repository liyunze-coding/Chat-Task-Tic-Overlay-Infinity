# Multi-Task Widget

### [Spanish translation / traducción al español](#Widget-de-Tareas-Múltiples)

Multi-Task Widget is a task widget that allows you to add multiple tasks at one time!

Want acccess to the multi-task widget for your stream? Buy it for $30 on <a href="https://ko-fi.com/s/94e7e8dc81" target="_blank">Ko-Fi</a>

Join the <a href="https://discord.gg/UnHyHkhbga" target="_blank">Discord server</a>!

---

# Follow the creator of the bot!

Hi! I frequently stream co-working & study so please drop a follow if you enjoy using the task bot, thank you <3

-   <a target="_blank" href="https://www.twitch.tv/RythonDev">https://twitch.tv/RythonDev</a>

If you like my work, consider supporting me on ko-fi!

-   <a target="_blank" href="https://www.ko-fi.com/Rython">https://ko-fi.com/Rython</a>

# Basic commands

`()` : optional, if there's only 1 incomplete task

`[]` : required

## Information

-   `!taskhelp` : shows the help message
-   `!check (@user)` : check (your own / mentioned user's) incomplete tasks
-   `!mytasks` : check your own incomplete tasks
-   `!list` : displays tasks as comma separated tasks to copy-paste
-   `!count (@user)` : check (your own / mentioned user's) completed task count
-   `!boardcount` : check how many tasks have been completed by everyone in chat
-   `!points (@user)` : check how many points (you / mentioned user) earned

## Tasks

-   `!add [task]` : add the task to your list
-   `!focus (index / task)` : focus on ONE task
-   `!edit (index) [task]` : edit the task at the specified index
-   `!done (index / task)` : mark task as done
-   `!undone (index / task)` : mark task as incomplete
-   `!remove (index / task)` : remove task from list
-   `!now [task]` : adds a new task and focuses on it
-   `!unfocus` : unfocuses on your focused task
-   `!clearmydone` : Clears all your completed tasks

### Mods only

-   `!adel [@user]` : deletes ALL the tasks of the specified user

## Points

-   `!points` : Clears all your completed tasks

### Admins only

-   `!addpoints @user <number>` : Adds a number of points to a specified user
-   `!reducepoints @user <number>` : Reduces a number of points from a specified user
-   `!setuserpoints @user <number>` : Set the number of points of the specified user

# Tips:

-   You can add/remove/mark complete several tasks in 1 command!
-   When you focus on a task, not only will it be highlighted on the widget, but the selected task will be given priority when you do `!done`

## Multiple tasks in one command

### Add command

-   `!add task 1, task 2, task 3`

### Remove command

-   `!remove task 1, task 2, task 3` (specify task names)
-   `!remove 1, 2, 3` (specify task indexes/indices)
-   `!remove 1 2 3` (specify task indexes/indices without commas)

### Done command

-   `!done task 1, task 2, task 3` (specify task names)
-   `!done 1, 2, 3` (specify task indexes/indices)
-   `!done 1 2 3` (specify task indexes/indices without commas)
-   `!done all`

# Admin commands

## Mod only

-   `!adel @user` : clears all tasks of the mentioned user

## Streamer only

-   `!clearall` : clears all tasks and count
-   `!cleartasks` : clears all tasks\*
-   `!cleardone` : clears all done tasks\*
-   `!clearns` : clear all tasks excluding streamer's incomplete tasks\*
-   `!resetboardcount` : resets the board count, individual users' task count won't be affected
-   `!resetuserscount` : resets users' task count, board count won't be affected
-   `!setboardcount [integer]` : set the board count to [integer], users' task count won't be affected

\* count won't be affected at all

---

Here is the Spanish translation of your text:

# Widget de Tareas Múltiples

¡El Widget de Tareas Múltiples es un widget que te permite agregar varias tareas al mismo tiempo!

¿Quieres acceso al widget de tareas múltiples para tu transmisión? Cómpralo en <a href="https://ko-fi.com/s/94e7e8dc81" target="_blank">Ko-Fi</a>

¡Únete al <a href="https://discord.gg/UnHyHkhbga" target="_blank">servidor de Discord</a>!

---

# ¡Sigue al creador del bot!

¡Hola! Frecuentemente hago transmisiones de co-working y estudio, así que por favor sígueme si disfrutas usar el bot de tareas, gracias <3

-   <a target="_blank" href="https://www.twitch.tv/RythonDev">https://twitch.tv/RythonDev</a>

Si te gusta mi trabajo, ¡considera apoyarme en Ko-fi!

-   <a target="_blank" href="https://www.ko-fi.com/Rython">https://ko-fi.com/Rython</a>

# Comandos básicos

`()` : opcional, si solo hay 1 tarea incompleta

`[]` : obligatorio

## Información

-   `!taskhelp` : muestra el mensaje de ayuda
-   `!check (@usuario)` : revisa las tareas incompletas (tuyas / del usuario mencionado)
-   `!mytasks` : revisa tus propias tareas incompletas
-   `!list` : muestra las tareas como una lista separada por comas para copiar y pegar
-   `!count (@usuario)` : revisa el conteo de tareas completadas (tuyas / del usuario mencionado)
-   `!boardcount` : revisa cuántas tareas han sido completadas por todos en el chat
-   `!points (@usuario)` : revisa cuántos puntos (tienes / tiene el usuario mencionado)

## Tareas

-   `!add [tarea]` : agrega la tarea a tu lista
-   `!focus (índice / tarea)` : enfócate en UNA tarea
-   `!edit (índice) [tarea]` : edita la tarea en el índice especificado
-   `!done (índice / tarea)` : marca la tarea como completada
-   `!undone (índice / tarea)` : marca la tarea como incompleta
-   `!remove (índice / tarea)` : elimina la tarea de la lista
-   `!now [tarea]` : agrega una nueva tarea y se enfoca en ella
-   `!unfocus` : desenfoca la tarea en la que estás enfocado
-   `!clearmydone` : elimina todas tus tareas completadas

### Solo moderadores

-   `!adel [@usuario]` : elimina TODAS las tareas del usuario especificado

## Puntos

-   `!points` : elimina todas tus tareas completadas

### Solo administradores

-   `!addpoints @usuario <número>` : agrega un número de puntos al usuario especificado
-   `!reducepoints @usuario <número>` : reduce un número de puntos del usuario especificado
-   `!setuserpoints @usuario <número>` : establece el número de puntos del usuario especificado

# Consejos:

-   ¡Puedes agregar/eliminar/marcar como completadas varias tareas en un solo comando!
-   Cuando te enfocas en una tarea, no solo se resaltará en el widget, sino que la tarea seleccionada tendrá prioridad cuando uses `!done`

## Varias tareas en un solo comando

### Comando de agregar

-   `!add tarea 1, tarea 2, tarea 3`

### Comando de eliminar

-   `!remove tarea 1, tarea 2, tarea 3` (especificar nombres de tareas)
-   `!remove 1, 2, 3` (especificar índices de tareas)
-   `!remove 1 2 3` (especificar índices de tareas sin comas)

### Comando de completar

-   `!done tarea 1, tarea 2, tarea 3` (especificar nombres de tareas)
-   `!done 1, 2, 3` (especificar índices de tareas)
-   `!done 1 2 3` (especificar índices de tareas sin comas)
-   `!done all`

# Comandos de administrador

## Solo moderadores

-   `!adel @usuario` : elimina todas las tareas del usuario mencionado

## Solo streamer

-   `!clearall` : elimina todas las tareas y el conteo
-   `!cleartasks` : elimina todas las tareas\*
-   `!cleardone` : elimina todas las tareas completadas\*
-   `!clearns` : elimina todas las tareas excepto las tareas incompletas del streamer\*
-   `!resetboardcount` : restablece el conteo del tablero, el conteo de tareas de los usuarios no se verá afectado
-   `!resetuserscount` : restablece el conteo de tareas de los usuarios, el conteo del tablero no se verá afectado
-   `!setboardcount [entero]` : establece el conteo del tablero en [entero], el conteo de tareas de los usuarios no se verá afectado

\* el conteo no se verá afectado en absoluto
